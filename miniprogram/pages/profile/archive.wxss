.notice {
  padding: 20rpx 24rpx;
  border-radius: 24rpx;
  background: var(--primary-soft);
  color: var(--primary);
  font-size: 24rpx;
}

.label {
  min-width: 150rpx;
}

.arrow {
  color: var(--ink-3);
  font-size: 40rpx;
}

.strong {
  font-weight: 700;
}
